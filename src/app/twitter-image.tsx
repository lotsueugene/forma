// Re-export the OG image as the Twitter card image — same dimensions
// (1200×630 works for both summary_large_image and OG), no duplication.
export { default, runtime, alt, size, contentType } from './opengraph-image';
